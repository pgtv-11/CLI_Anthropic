{
  "name": "@avenue/kyc-aml-service",
  "version": "0.1.0",
  "private": true,
  "description": "KYC / AML / CIP service (M1).",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx ./src/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'todo: eslint'"
  },
  "dependencies": {
    "@avenue/audit-sdk": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
